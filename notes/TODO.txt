1. Categories of fonts (2 tables with foreign keys)
  - DONE

2. Google API
  key:  AIzaSyCqZ3dPYustmQUajzIhYu7MJeJ_ePHPnyk    /   DiveInnFontTest
  append the query parameter key=yourAPIKey to all request URLs

  - service to get all the fonts
  - data cached (only make the call once per session) -- use NgRx(??)
  - list of checkboxes, paginated, listing all the fonts
      selecting checkbox adds font to db
      or button to add
      searchable

  - UI controls for passing a font back
    google data
    our extra data
      bold(??)
      font is header or text

  - UI to show selected fonts, button to remove(?)

  - blacklist fonts so they don't show up in all fonts selection
    separate table (?) or flag on font object (?)





  -------
  0. rename fontsRouter/testDataRouter
  0a. remove magic strings from font.service
  1. figure out why intellisense sucks
  2. look into GraphQL
  3. NgRx
  4. User login / authentication
  5. figure out how/where to store credentials
    db
    google API



------
Cache X number of sort methods
  Popularity, trending
  try sorting ourselves for alpha, style (category)

Dropdown / radio buttons of sort options

List of fonts, paginated
  some way to select how many fonts per page

Buttons(?) for add to favorites or blacklist


Style option checkmarks ?
  italic ? bold ?


